name: transactions_tracker
description: A transaction tracking application with import/export capabilities.
version: 1.0.0

environment:
  sdk: ^3.7.0-274.0.dev

dependencies:
  csv: ^5.0.3
  excel: ^2.0.0
  json: ^0.20.4

dev_dependencies:
  lints: ^5.0.0
  test: ^1.24.0
