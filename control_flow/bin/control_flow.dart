// Control flow in Dart:
// 1. Conditional Statements:
// - If and Else
// - Switch and Case
// 2. Loops:
// - For loop
// - For-in loop
// - While loop
// - Do - While loop
// 3. Control Flow Operators
// - Break
// - Continue
// 4. Exception Handling
// Dart uses try, catch, on, and finally blocks to handle exceptions.
// The try block contains code that may throw an exception, and catch handles it.

// 5. Return Statements

// Go details:

// 1. Conditional Statements:
//  If and else:
import 'dart:math';

// void main() {
/*   double weight = 50;
  double height = 158;
  double bmi = weight / pow((height / 100), 2);

  String status = "";

  if (bmi < 18.5) {
    status = "Underweight";
  } else if (bmi >= 18.5 && bmi <= 24.9) {
    status = "Healthy Weight";
  } else if (bmi >= 25 && bmi <= 29.9) {
    status = "Overweight";
  } else {
    status = "Obesity";
  }

  print("BMI $bmi: Status: $status");
} */

// Switch and Case:
/* void main() {
  String grade = 'A';
  switch (grade) {
    case 'A':
      print("Excellent!");
      break; // 'break 'at the end of each case to prevent "fall-through"
    case 'B':
      print("Good!");
      break;
    default:
      print("Try harder next time!");
  } // Closing brace for switch
} // Closing brace for main */


// 2. Loops
